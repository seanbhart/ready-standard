schema: readyroom/primitive/v1
kind: primitive
id: I-001
type: intent
primitive_class: product_logic
compiler_role: source_truth
title: "{{INTENT_TITLE}}"
milestone: "{{MILESTONE_ID}}"
status: draft
fields:
  statement: "{{AS_ACTOR_I_CAN_OUTCOME_SO_VALUE}}"
  intent_type: "{{INTENT_TYPE_FROM_READY_VOCABULARY}}"
  claim_level: "{{CONCEPT_PROCESS_PROOF_SIMULATION_BASELINE_FIXTURE_VALIDATED_HUMAN_LABELED_VALIDATED_PRODUCTION_MONITORED}}"
  actor: "{{PRIMARY_ACTOR}}"
  trigger: "{{WHEN_THIS_PROMISE_STARTS}}"
  action: "{{WHAT_THE_ACTOR_OR_SYSTEM_DOES}}"
  expected_end_state: "{{OBSERVABLE_DONE_STATE}}"
  value: "{{WHY_THIS_MATTERS}}"
  scope: "{{WHAT_IS_INCLUDED}}"
  non_scope: "{{WHAT_IS_EXCLUDED}}"
  failure_behavior: "{{WHAT_HAPPENS_WHEN_IT_CANNOT_WORK}}"
  process_decision: "{{CURRENT_PROCESS_OR_NONE}}"
  validation_boundary: "{{VALIDATION_LIMITS_OR_NONE}}"
  input_types: "{{INPUT_TYPES_OR_REQUIRED_CONTEXT}}"
  input_samples: "{{INPUT_SAMPLE_ARTIFACT_IDS_OR_NONE}}"
  output_types: "{{OUTPUT_TYPES_OR_STATE_CHANGES}}"
  output_samples: "{{OUTPUT_SAMPLE_ARTIFACT_IDS_OR_NONE}}"
  required_surfaces: "{{USER_OPERATOR_OR_INTERNAL_SURFACES_REQUIRED}}"
  operating_envelope: "{{QUALITY_OR_LIVE_VARIABLE_OPERATING_LIMITS_OR_NONE}}"
refs: []
artifacts: []
owner_notes: []
