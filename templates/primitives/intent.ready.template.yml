schema: readyroom/primitive/v1
kind: primitive
id: I-001
type: intent
primitive_class: product_logic
compiler_role: source_truth
title: "{{INTENT_TITLE}}"
milestone: "{{MILESTONE_ID}}"
status: draft
summary: "{{INTENT_SUMMARY}}"
fields:
  statement: "{{AS_ACTOR_I_CAN_OUTCOME_SO_VALUE}}"
  value: "{{WHY_THIS_MATTERS}}"
  scope: "{{WHAT_IS_INCLUDED}}"
  non_scope: "{{WHAT_IS_EXCLUDED}}"
  failure_behavior: "{{WHAT_HAPPENS_WHEN_IT_CANNOT_WORK}}"
  input_data: "{{INPUTS_OR_REQUIRED_CONTEXT}}"
  output_data: "{{OUTPUTS_OR_STATE_CHANGES}}"
  required_services: "{{SERVICE_IDS_OR_NONE}}"
  required_standards: "{{STANDARD_IDS_OR_NONE}}"
refs: []
artifacts: []
owner_notes: []
