schema: ready/artifact/v1
kind: primitive
id: AM-001
class: artifact
type: manifest
compiler_role: build_input
title: "{{MANIFEST_ARTIFACT_TITLE}}"
status: ready
privacy: "{{safe_ref_or_public_resource|private_ref}}"
retrieved_at: "{{YYYY-MM-DD_OR_GENERATED_AT}}"
fields:
  confidence: "{{low|medium|high}}"
  provenance: "{{WHO_AUTHORED_OR_GENERATED_THIS_MANIFEST}}"
  purpose: "{{ONE_PURPOSE_THIS_MANIFEST_ARTIFACT_SERVES}}"
  content: "{{WHAT_THIS_MANIFEST_OR_INDEX_CONTAINS}}"
  intended_use: "{{HOW_AGENTS_OR_TOOLS_SHOULD_USE_THIS_MANIFEST_ARTIFACT}}"
  format: "{{yaml|json|markdown|external_ref}}"
  manifest_scope: "{{PACKAGE_DATASET_ARTIFACT_GROUP_OR_TOOL_INDEX_SCOPE}}"
files:
  - path: "{{READY_RELATIVE_OR_EXTERNAL_SAFE_REF}}"
    role: "{{primary|version|supporting}}"
    purpose: "{{HOW_THIS_FILE_SERVES_THE_MANIFEST_ARTIFACT}}"
usage_policy: []
owner_notes: []
