schema: readyroom/artifact/v1
kind: artifact
id: A-001
type: "{{customer_statement|source_document|reference_resource|access_reference|review_protocol|proof_corpus|code_snippet|design_artifact|manifest}}"
primitive_class: evidence_artifact
compiler_role: evidence
title: "{{ARTIFACT_TITLE}}"
milestone: "{{MILESTONE_ID}}"
status: active
privacy: "{{safe_ref_or_public_resource|sanitized_fixture|private_ref}}"
retrieved_at: "{{YYYY-MM-DD_OR_GENERATED_AT}}"
fields:
  confidence: "{{low|medium|high}}"
  provenance: "{{HOW_THIS_ARTIFACT_OR_PAYLOAD_WAS_OBTAINED_OR_AUTHORED}}"
  purpose: "{{ONE_PURPOSE_THIS_DESCRIPTOR_EXISTS_TO_SERVE}}"
  content: "{{WHAT_THIS_ARTIFACT_CONTAINS}}"
  intended_use: "{{HOW_PRIMITIVES_OR_AGENTS_SHOULD_USE_THIS_ARTIFACT}}"
  format: "{{yaml|json|jsonl|markdown|html|png|svg|text|external_ref|mixed}}"
files:
  - path: "{{READY_RELATIVE_OR_EXTERNAL_SAFE_REF}}"
    role: "{{primary|version|partition|representation|supporting}}"
    purpose: "{{HOW_THIS_FILE_SERVES_THE_DESCRIPTOR_PURPOSE}}"
usage_policy: []
owner_notes: []
