schema: ready/artifact/v1
kind: primitive
id: AR-001
class: artifact
type: customer_statement
compiler_role: evidence
title: "{{CUSTOMER_STATEMENT_TITLE}}"
status: ready
privacy: "{{user_provided_customer_statement|sanitized_excerpt|private_ref}}"
retrieved_at: "{{YYYY-MM-DD_OR_GENERATED_AT}}"
fields:
  confidence: "{{low|medium|high}}"
  provenance: "{{HOW_THIS_STATEMENT_WAS_OBTAINED}}"
  purpose: "{{ONE_PURPOSE_THIS_STATEMENT_SERVES}}"
  content: "{{WHAT_THIS_STATEMENT_CONTAINS}}"
  intended_use: "{{HOW_PRIMITIVES_OR_AGENTS_SHOULD_USE_THIS_STATEMENT}}"
  format: "{{markdown|text|external_ref}}"
  statement: "{{CUSTOMER_OR_USER_STATEMENT}}"
  speaker_type: "{{user|buyer|operator|customer|support|internal_stakeholder}}"
files:
  - path: "{{READY_RELATIVE_OR_EXTERNAL_SAFE_REF}}"
    role: "{{primary|supporting}}"
    purpose: "{{HOW_THIS_FILE_SERVES_THE_STATEMENT}}"
usage_policy: []
owner_notes: []
