---
title: "Product Tree Standard"
description: "The source-of-truth rules for a Git-backed product tree."
---

# Product Tree Standard

A product tree is a committed filetree that stores product truth in typed
records. Primitive source records use `.ready.yml`: plain YAML with a distinct
extension so importers know exactly how to parse it. The tree should behave like
a healthy codebase: fragmented enough to review, explicit enough to validate,
and structured enough to compile into different views.

## Source of truth

Each primitive owns its durable truth in one `.ready.yml` file:

- The YAML document is the machine contract.
- Apps and docs compile reader-friendly views from the YAML.
- Stable ids are the durable identity.
- Paths and titles are refactorable.
- Generated views derive from the tree.

Markdown is still useful for navigation, governance, and generated views, but it
is not the primitive source format. Top-level index files are allowed for
navigation, but they must not duplicate record truth.

## Stages

The root `ready/manifest.yaml` owns the stage registry. Stage directories may be
named `m1`, `m2`, `alpha`, `client-pilot`, or another project-specific id, but
tools should use manifest metadata instead of inferring meaning from directory
names or sort order.

Use `stages` in the root manifest:

```yaml
default_stage: m1
stages:
  - id: m1
    title: M1
    path: ready/m1
    manifest: ready/m1/manifest.yaml
    kind: normal
    status: active
    order: 10
    default_candidate: true
    coding_claims_enabled: false
  - id: end-state
    title: End State
    path: ready/end-state
    manifest: ready/end-state/manifest.yaml
    kind: horizon
    status: horizon
    order: 9999
    default_candidate: false
    coding_claims_enabled: false
```

Stage kinds:

- `normal`: buildable product stage.
- `horizon`: future product concept shelf, not claimable implementation work.
- `experiment`: alternative branch or product plan.
- `archive`: historical stage, not default-selectable.
- `template`: reusable starter shape.

Default selection is deterministic:

1. Use `default_stage` when it points to a valid stage.
2. Otherwise choose the highest `order` stage where `kind: normal` and
   `default_candidate: true`.
3. Otherwise choose the highest `order` stage where `kind: normal`.
4. Only fall back to horizon, experiment, archive, or template stages when no
   normal stage exists.

`end-state` is not special because of its name. It is excluded from the normal
default path because it is `kind: horizon` and `default_candidate: false`.

## Primitive types

The core primitive types are:

- `premise`: why the product, milestone, or feature should exist.
- `intent`: what the product promises to do.
- `standard`: how the product must be built, measured, judged, or maintained.
- `service`: what the product depends on to build, prove, run, or monitor.
- `question_card`: unresolved product-shaping ambiguity.

The product may add more record kinds later, but these types are enough to
compile useful product, design, and implementation views.

## Governance and artifacts

Project-specific governance documents live under `ready/governance/`.
Reusable Ready resources, task-skill packages, and templates live in the Ready
standard package. Together they constrain how product truth is created,
changed, approved, and handed to agents.

Artifacts live under milestone artifact directories such as
`ready/m1/artifacts/`. They support primitives and proof with samples, snippets,
design assets, manifests, screenshots, and handoff notes.

## Generated views

Compiled docs, work packages, designer briefs, PM summaries, customer-facing
explanations, and app views are projections of the product tree. They can be
cached or committed only when marked generated and traceable to source records.

A human-authored document may propose a product change, but the accepted change
must be applied back to the product tree.

## Non-goals

The product tree is not a backend replacement forever. It is the first durable
source of truth. A backend can later index it, render it, coordinate live team
state, and host agents without changing the core product model.
