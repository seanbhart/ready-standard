---
title: "Directory Layout"
description: "Recommended filetree layout for a Ready Product Tree."
---

# Directory Layout

A product tree should keep records fragmented by type and stage. The root
manifest declares the stage registry; tools should not infer stage order or
default selection from directory names.

```text
ready/
  README.md
  manifest.yaml
  governance/
    agent-guidelines.md
    product-orchestrator-charter.md
    product-process.md
    workspace-authority-and-access.md
  end-state/
    README.md
    manifest.yaml
  m1/
    README.md
    manifest.yaml
    premises.md
    premises/
      p-001-example.ready.yml
    intents.md
    intents/
      i-001-example.ready.yml
    standards.md
    standards/
      st-001-example.ready.yml
    services.md
    services/
      sv-001-example.ready.yml
    flags.md
    flags/
      seed/
        f-001-example.md
      delta/
      discovery/
    artifacts.md
    artifacts/
      manifest.yaml
      samples/
        MANIFEST.json
        SA-001.json
      resources/
      snippets/
      designs/
      manifests/
```

Top-level plural Markdown files are navigation indexes. Product-logic primitive
source records live inside the directories as `.ready.yml` files. Other
primitive classes can live in their own family directories, such as `flags/`,
`questions/`, `artifacts/`, or `governance/`, with schemas that match their
machine contract.

`ready/manifest.yaml` should include `default_stage` and a `stages` list with
`id`, `title`, `path`, `manifest`, `kind`, `status`, `order`,
`default_candidate`, and `coding_claims_enabled`. Use `kind: normal` for
buildable stages and `kind: horizon` for `end-state` or other future concept
shelves.

Project-specific governance primitives belong under `ready/governance/`.
Reusable Ready resources, task-skill packages, and templates belong in the Ready
standard package and may be vendored only when a project needs an offline copy.
Evidence/artifact primitives belong under milestone artifact directories, while
large or sensitive payloads may be stored as safe refs. Products may add
`compiled/` or `views/`, but generated outputs must be clearly marked and
traceable to source record hashes.
