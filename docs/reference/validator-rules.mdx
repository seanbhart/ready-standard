---
title: "Validator Rules"
description: "Minimum validation checks for a product-tree importer or docs compiler."
---

# Validator Rules

A product-tree importer should fail loudly when source truth is ambiguous.

## Required checks

- Every primitive source record is a `.ready.yml` YAML document.
- Every primitive source record has `schema: readyroom/primitive/v1`.
- Every primitive source record has `kind: primitive`.
- Every primitive source record has a stable `id`.
- No two records share the same `id`.
- Primitive `type` is valid.
- Directed refs point to existing ids.
- Ref roles are approved.
- Generated indexes do not carry independent primitive truth.
- Seed and delta flags are non-claimable unless they pass the claim gate.
- Claimable flags carry Completion Proof.
- `ready` flags with non-empty `blocked_by` remain non-claimable.
- Primitive files do not carry implementation lifecycle state.
- Project-specific governance docs are stored under `ready/governance/`, not artifact directories.
- Project-agnostic Ready resources and templates are referenced from the Ready standard package or an explicitly vendored copy, not artifact directories.
- Artifact ids are unique within their artifact family.
- Sensitive content, source excerpts, diffs, raw logs, and secrets are absent.
- Large artifacts use storage paths or external refs instead of inline payloads.

## Warning checks

- Primitive body is too large to review comfortably.
- Premise confidence is low without a discovery flag or question card.
- Intent lacks failure behavior.
- Intent lacks required services or standards.
- Quality-bearing intent lacks an Operating Envelope.
- Service readiness is unknown without a blocker.
- Product-shaping ambiguity appears only in prose.

## Importer behavior

Validators should report exact file paths and ids. They should not silently fix
semantic errors, average conflicting docs, or promote open flags into coding
workflow.
